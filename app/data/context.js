define(['data/user', 'data/event', 'data/category'],
    function(user, event, category){

        return{
            user: user,
            event: event,
            category: category
        };
    });
